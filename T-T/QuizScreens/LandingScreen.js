/* import { NavigationContainer, useNavigation } from '@react-navigation/native';
import { createStackNavigator } from "@react-navigation/stack";
import { Section1 } from './QuizSection1';
import { NavigateTo, NavigateApp } from '../NavigateApp';
import HomeScreen from '../screens/HomeScreen'; */
/* 
import React, {useState} from 'react';
import { StyleSheet, Text, View, Button, ImageBackground, Image} from 'react-native';

const imageSource = "../assets";

export function LandingScreen(){
    return(
        <View style ={styles.container}>
            <LinearGradient colors={['rgba(223, 238, 235, 0.8)', 'transparent']} style={styles.background}/>
            <Image source= {require(imageSource)} style={{top: "22%"}}/>
            <Text style= {styles.appName}>turtlGainz</Text>
            <View style ={styles.button}>
                <Button color={'rgb(81, 130, 135)'} title= "Get Started"  />
                <Button color={'rgb(81, 130, 135)'} title="Log in"  /> 
            </View>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: "rgb(100, 181, 190)",
      flexDirection: 'column',
      alignItems: "center",
    },
    button: {
        flex:.2,
        width: "50%",
        top: "25%",
        marginHorizontal: "20%",
        justifyContent: 'space-between',
    },
    appName: {
        fontSize: 40,
        top:"23%",
        fontWeight: 'bold',
    },
    background: {
        position: "absolute",
        left: 0,
        right: 0,
        top: 0,
        height: 800,
      }
  }); */